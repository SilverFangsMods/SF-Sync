{
  "name": "sf-sync-lock",
  "private": true,
  "version": "0.1.0",
  "description": "Autoridade de lock read-only distribuido do SF-Sync (Durable Object)",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4",
    "wrangler": "^3",
    "typescript": "~5.8.3"
  }
}
