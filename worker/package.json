{
  "name": "sf-sync-lock",
  "private": true,
  "version": "0.1.0",
  "description": "SF-Sync worker (API e lock)",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4",
    "wrangler": "^3",
    "typescript": "~5.8.3"
  }
}
