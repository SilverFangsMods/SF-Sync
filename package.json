{
  "name": "sf-sync",
  "private": true,
  "version": "0.1.0",
  "description": "Sincronizador de pastas multi-inquilino — app de bandeja com rclone bisync e backend serverless.",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "tsc && vite build",
    "preview": "vite preview",
    "tauri": "tauri"
  },
  "dependencies": {
    "@tauri-apps/api": "^2",
    "qrcode": "^1.5.4"
  },
  "devDependencies": {
    "@tauri-apps/cli": "^2",
    "@types/qrcode": "^1.5.6",
    "typescript": "~5.8.3",
    "vite": "^7.0.4"
  }
}
